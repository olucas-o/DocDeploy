# Public browser origin allowed by the API.
CORS_ORIGIN=http://localhost:5173

# Host port for the API when using Docker Compose.
API_PORT=3000

# Leave empty for local health checks. Never commit a real key.
OPENAI_API_KEY=
